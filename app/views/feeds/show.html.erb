<p id="notice"></p>
<h1>投稿詳細</h1>
  <table>
    <p>ユーザー: <%= @feed.user.name %></p>
    <p>タイトル: <%= @feed.title %></p>
    <p>内容: <%= @feed.content %></p>
    <p>画像: </p>
      <%= image_tag @feed.image.url %> 
  </table>

<%= link_to '編集する', edit_feed_path(@feed) %> |
<%= link_to '投稿一覧へ', feeds_path %>
